<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blogspot.yourfavoritekaisar.ourclass">


    <application
        android:allowBackup="true"
        android:icon="@drawable/ourclass"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ourclass"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".splash_screen_activity" />
        <activity android:name=".RecycleActivity" />
        <activity android:name=".detail_siswa_Activity"></activity>
    </application>

</manifest>